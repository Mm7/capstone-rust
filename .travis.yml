before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libcapstone-dev libcapstone3

language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
